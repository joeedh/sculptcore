#pragma once

// Generated by CMake (configure_file) from remesh_cli_config.h.in. Embeds
// build-time paths + the configured git commit as string literals (no -D string
// defines, which the sccache launcher mangles on Windows). The commit is the one
// present when CMake last configured, not necessarily HEAD at run time.
#define REMESH_CLI_GIT_COMMIT  "@REMESH_CLI_GIT_COMMIT@"
#define REMESH_CLI_ASSETS_DIR  "@REMESH_CLI_ASSETS_DIR@"
#define REMESH_CLI_RESULTS_DIR "@REMESH_CLI_RESULTS_DIR@"
