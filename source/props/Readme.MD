This folder contains a dynamic property system.  It supports tagging
properties with a bunch of UI constraints and data (e.g. numeric ranges, tooltips)
and processing input devices such as pen pressure, tilt, etc.

While litestl's binding system is meant to describe raw C++ classes at compile time,
this is a purely runtime system meant to communicate with a host application frontend.
