# Embed the assets dir as a string literal via a generated header rather than a
# command-line -D"..." define: the cross-worktree sccache launcher mangles the
# quote escaping of string-literal defines on Windows, so the macro would expand
# unquoted and fail to compile.
set(SCULPTCORE_ASSETS_DIR_CMAKE "${CMAKE_CURRENT_SOURCE_DIR}/assets")
set(SCULPTCORE_KERNELS_DIR_CMAKE "${CMAKE_SOURCE_DIR}/source/brush/kernels")
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/test_config.h.in
  ${CMAKE_CURRENT_BINARY_DIR}/test_config.h @ONLY)

macro(test file extra_lib)
set(target "${file}_out")
add_executable(${target} "${file}")
target_link_libraries(${target} util platform ${extra_lib} math)
target_compile_options(${target} PRIVATE -DNO_PARALLEL_FOR)
target_include_directories(${target} PUBLIC ${CMAKE_CURRENT_BINARY_DIR})
target_include_directories(${target} PUBLIC util)
target_include_directories(${target} PUBLIC math)
target_include_directories(${target} PUBLIC mesh)
target_include_directories(${target} PUBLIC binding_generators)
target_include_directories(${target} PUBLIC binding)
add_test(NAME ${target} COMMAND ${target})
endmacro()

test(test_mesh.cc "mesh")
test(test_mesh_arrays.cc "mesh")
test(test_select_derive.cc "mesh")
test(test_elem_alloc.cc "mesh")
test(test_delaunay.cc "mesh")
test(test_constrained_delaunay.cc "mesh")
test(test_edge_collapse.cc "mesh")
test(test_collapse_repro.cc "mesh")
test(test_collapse_fuzz.cc "mesh")
test(test_edge_split.cc "mesh")
test(test_dyntopo.cc "mesh")
test(test_edge_flip.cc "mesh")
test(test_attr_saver.cc "mesh")
test(test_attr_merge.cc "mesh")
test(test_deform_attr.cc "mesh")
test(test_weights_attr.cc "mesh")
test(test_props.cc "props")
test(test_props_curve.cc "props")
test(test_meshlog_topo.cc "meshlog;mesh;spatial")
test(test_meshlog_preview_unbound.cc "meshlog;mesh;spatial")
test(test_dyntopo_undo.cc "meshlog;mesh;spatial")
test(test_mesh_reorder.cc "meshlog;mesh;spatial")
test(test_weights_undo.cc "meshlog;mesh;spatial")
test(test_spatial_reorder_inc.cc "spatial;mesh")
test(test_mesh_topo_cache.cc "mesh")
test(test_boundary.cc "mesh")
test(test_boundary_serialize.cc "mesh")
test(test_uvgen.cc "mesh")
test(test_uv_reproject.cc "mesh")
test(test_mesh_serialize.cc "mesh")
test(test_spatial_raycast.cc "spatial;mesh")
test(test_spatial_displacement_bounds.cc "spatial;mesh")
test(test_sculpt_layers.cc "displace;mesh")
test(test_frame_provider.cc "displace;mesh")
test(test_subdiv.cc "subdiv;mesh")
test(test_grids_store.cc "subdiv;mesh")
test(test_multires.cc "subdiv;spatial;displace;mesh;vdm")
test(test_grid_domain.cc "subdiv;spatial;displace;mesh;vdm")
test(test_grid_stroke.cc "brush;subdiv;meshlog;spatial;displace;mesh;vdm")
test(test_vdm_store.cc "vdm;mesh")
test(test_vdm_gpu.cc "vdm;mesh")
test(test_spatial_pick.cc "spatial;mesh")
test(test_spatial_gpu_partition.cc "spatial;mesh")
test(test_spatial_dyntopo.cc "spatial;mesh")
test(test_spatial_merge.cc "spatial;mesh")
test(test_dyntopo_cascade.cc "mesh")
test(test_dyntopo_budget.cc "mesh")
test(test_dyntopo_smooth.cc "mesh")
test(test_dyntopo_trace.cc "mesh")
test(test_dyntopo_features.cc "mesh")
test(test_dyntopo_uvchart.cc "mesh")
test(test_edge_split_boundary.cc "mesh")
test(test_remesh_scaffold.cc "remesh;mesh;spatial")
test(test_remesh_curvature.cc "remesh;mesh;spatial")
test(test_remesh_curvature_filter.cc "remesh;mesh;spatial")
test(test_remesh_density.cc "remesh;mesh;spatial")
test(test_remesh_field.cc "remesh;mesh;spatial")
test(test_remesh_singularity.cc "remesh;mesh;spatial")
test(test_remesh_param.cc "remesh;mesh;spatial")
test(test_remesh_quantize.cc "remesh;mesh;spatial")
test(test_remesh_extract.cc "remesh;mesh;spatial")
test(test_remesh_metrics.cc "remesh;mesh;spatial")
test(test_remesh_triage.cc "remesh;mesh;spatial")
test(test_remesh_preremesh.cc "remesh;mesh;spatial")
test(test_remesh_feature_filter.cc "remesh;mesh;spatial")
test(test_remesh_retry.cc "remesh;mesh;spatial")
test(test_remesh_preset.cc "remesh;mesh;spatial")
test(test_brush.cc "binding_generators")
test(test_stroke_driver.cc "brush;spatial;mesh")
test(test_brush_metadata.cc "brush;spatial;mesh")
test(test_brush_registry.cc "brush;spatial;mesh")

# Drives the sbrush compiler (lexer/parser/emitters) directly; the compiler
# lib only exists when the host tool builds (SBRUSH_BACKEND_CPP).
if (TARGET sbrushc_core)
test(test_sbrush_textures.cc "sbrushc_core")
test(test_sbrush_attr_writes.cc "sbrushc_core")
# The C99 backend end-to-end: emitCTextureDefs -> tinycc JIT -> parity
# against the precompiled texture registry (texture-scripts T3.2).
test(test_texture_c_jit.cc "sbrushc_core;brush;tinycc")
# The runtime program surface over the same pipeline (T3.3).
test(test_texture_program.cc "brush")
# Brush-level binding + a tcc-vs-precompiled stroke A/B (T3.4).
test(test_texture_stroke.cc "brush;spatial;mesh")
endif()

# The embedded tinycc JIT (texture-scripts T3.1): capability probe + the
# -nostdlib/tcc_add_symbol strategy the runtime texture compiler uses.
test(test_tcc_jit.cc "brush;tinycc")

if (WITH_ABSEIL)
test(test_map_bench.cc "absl::flat_hash_map")
endif()

test(test_binding.cc "binding_generators")

# These tests link debug_core, which pulls in the Vulkan backend; only build
# them when WITH_VULKAN is on (the debug app / interactive GPU path).
if (WITH_VULKAN)
test(test_debug_script.cc "debug_core")
test(test_live_stroke.cc "debug_core")
test(test_dyntopo_stroke_undo.cc "debug_core")
test(test_meshlog_preview_rollback.cc "debug_core")
test(test_dyntopo_collapse_crash.cc "debug_core")
test(test_dyntopo_undo_nonnewest.cc "debug_core")
test(test_dyntopo_multistep_gpu.cc "debug_core")
test(test_spatial_update_split.cc "debug_core")
test(test_brush_attr.cc "debug_core")
test(test_paint_undo.cc "debug_core")
test(test_layer_stroke_undo.cc "debug_core")
test(test_multires_stroke.cc "debug_core")
test(test_grid_gpu.cc "debug_core")
test(test_vdm_stroke.cc "debug_core")
test(test_vdm_promote.cc "debug_core")
test(test_brush_nonaccum.cc "debug_core")
test(test_brush_noise.cc "debug_core")
test(test_dyntopo_nonaccum_collapse.cc "debug_core")
test(test_brush_mirror_grab.cc "debug_core")
test(test_grab_region.cc "debug_core")
test(test_wingscrape.cc "debug_core")
test(test_snakehook.cc "debug_core")
test(test_gpu_brush_seam.cc "debug_core")
test(test_unbounded_seam.cc "debug_core")
test(test_brush_props.cc "debug_core")
test(test_brush_dynamics.cc "debug_core")
test(test_brush_uniform_validate.cc "debug_core")
test(test_brush_falloff.cc "debug_core")
test(test_bsmooth.cc "debug_core")
test(test_enhance.cc "debug_core")
test(test_automask_stroke.cc "debug_core")
test(test_automask_view_normal.cc "debug_core")
test(test_automask_gpu.cc "debug_core")
test(test_spatial_boundary_normals.cc "debug_core")
endif()

test(test_automask_cavity.cc "mesh")
test(test_seam_analysis.cc "mesh")
test(test_uniform_link.cc "gpu")
test(test_webgpu_render.cc "webgpu;spatial;mesh;gpu")
# wgpu-native ships as a DLL — copy it next to the test exe so it loads at run.
add_custom_command(TARGET test_webgpu_render.cc_out POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E copy_if_different
    $<TARGET_FILE:wgpu_native> $<TARGET_FILE_DIR:test_webgpu_render.cc_out>
  VERBATIM)
test(test_stencil_gpu.cc "webgpu;subdiv;spatial;mesh;gpu")
add_custom_command(TARGET test_stencil_gpu.cc_out POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E copy_if_different
    $<TARGET_FILE:wgpu_native> $<TARGET_FILE_DIR:test_stencil_gpu.cc_out>
  VERBATIM)
#test(test_constexpr_bug.cc "")
